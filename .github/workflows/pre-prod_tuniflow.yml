name: Deploy Fullstack App (Backend + Frontend) to Azure

on:
  push:
    branches:
      - pre-prod

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'  # Active le cache pour accélérer l'installation des dépendances
          cache-dependency-path: '**/package-lock.json'

      - name: Install backend dependencies
        working-directory: backend
        run: npm install

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm install

      - name: Build frontend into backend
        working-directory: frontend
        run: |
          npm run build
          cp -r dist ../backend/public  # Remplacer 'build' par 'dist' et copier dans backend/public

      - name: Deploy to Azure
        uses: azure/webapps-deploy@v2
        with:
          app-name: TuniFlow
          slot-name: Production
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE }}
          package: backend  # Assurez-vous que ce chemin contient bien le dossier 'public' avec les fichiers frontend